-- All this does is set up the database needed for the sequelize models
-- I found it useful to save this code in MySQL when changing the tables.
DROP DATABASE IF EXISTS minion_mingle_db;

CREATE DATABASE minion_mingle_db;